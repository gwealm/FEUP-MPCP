#include <stdio.h>                                                              
#include <stdlib.h>                                                             

extern int SOMA_V(int *baseAddr, int size);

int main(void) {
	/* 1
	const int DIM = 5;
	int v[] = {3, -1, 8, 0, -3};
	int sum = SOMA_V(v, dim);
	printf("Sum of the elements: %d\n", sum);
    */

	return EXIT_SUCCESS;
}
