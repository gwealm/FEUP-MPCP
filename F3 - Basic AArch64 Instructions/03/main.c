#include <stdio.h>
#include <stdlib.h>

extern void COUNT_ONES();
extern void EQUAL_BITS();
extern int ContPar(long *array, long size);
extern void CountUpper();

int main(void) {
	//COUNT_ONES();
	//EQUAL_BITS();

	/*		Exercise 3d)
	    long size = 0;
            long array[] = {};
            long evens = ContPar(array, size);
            printf("Resultado: %ld", evens);
	*/

	//CountUpper();

	return EXIT_SUCCESS;
}
